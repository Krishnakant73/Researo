# Researo API — full dependency list (core + AI extras).
# A single `pip install -r requirements.txt` installs everything needed to run
# in production (Railway/Docker/Nixpacks) including local embeddings, the
# cross-encoder reranker, ChromaDB and token counting.
#
# torch is resolved from the PyTorch CPU wheel index to avoid the multi-GB
# CUDA build. This line must stay at the top.
--extra-index-url https://download.pytorch.org/whl/cpu

# ---------------------------------------------------------------------------
# Core web framework
# ---------------------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
python-multipart==0.0.20
orjson==3.10.12

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
sqlalchemy==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0          # async Postgres driver (production / Railway)
alembic==1.14.0
greenlet==3.1.1          # required by SQLAlchemy async engine

# ---------------------------------------------------------------------------
# LLM Gateway
# ---------------------------------------------------------------------------
httpx==0.28.1
tenacity==9.0.0

# ---------------------------------------------------------------------------
# LangGraph / LangChain (multi-agent orchestrator + providers)
# ---------------------------------------------------------------------------
langgraph==0.2.60
langchain-core==0.3.28
langchain==0.3.13
langchain-openai==0.2.14
openai==1.58.1

# ---------------------------------------------------------------------------
# Retrieval — vector store + BM25 + embeddings + reranker
# ---------------------------------------------------------------------------
numpy>=1.26,<3
rank-bm25==0.2.2
chromadb>=0.5,<0.6
sentence-transformers==3.3.1
tiktoken==0.8.0

# ---------------------------------------------------------------------------
# PDF / DOCX / spreadsheet parsing
# ---------------------------------------------------------------------------
pymupdf==1.25.1
pdfplumber==0.11.4
openpyxl==3.1.5

# ---------------------------------------------------------------------------
# Utility
# ---------------------------------------------------------------------------
python-dotenv==1.0.1
loguru==0.7.3
